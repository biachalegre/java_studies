public class Main {
    public static void main(String[] args) {
        // Create a new object in the class StudentGrades (instantiate)
        StudentGrades studentGrades = new StudentGrades();

        // Calls the method inputStorage through the object studentGrades
        studentGrades.inputStorage();

        // Calls the method calculateStudentResults through the object studentGrades
        studentGrades.calculateStudentsResults();

    }
}
